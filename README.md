# 0xTheo

Security blog — malware analysis, reverse engineering, Windows internals.

→ [ttlabrindille.github.io](https://ttlabrindille.github.io)

## Setup local

```bash
gem install jekyll bundler
bundle install
bundle exec jekyll serve
# → http://localhost:4000
```
